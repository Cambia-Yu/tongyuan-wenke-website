(()=>{const track=document.getElementById('track'),video=document.getElementById('video'),copy=document.getElementById('heroCopy'),wash=document.getElementById('wash'),shade=document.getElementById('shade'),nav=document.getElementById('nav'),hint=document.getElementById('hint'),stage=document.getElementById('serviceStage'),services=document.getElementById('services');const DURATION=5.041667;const clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v));const smooth=t=>t*t*(3-2*t);const map=(p,a,b)=>smooth(clamp((p-a)/(b-a)));const mix=(a,b,t)=>a+(b-a)*t;const RESET_P=.90,SLOW_HANDOFF_MS=1050,FAST_HANDOFF_MS=820,WHEEL_GESTURE_END_MS=220,NEW_GESTURE_GAP_MS=85;let handoff=0,handoffRunning=false,handoffDone=false,raf=0,handoffRaf=0,velocityEMA=0,lastInputAt=0,quarantine=false,quarantineTimer=0,lastBlockedWheelAt=0,queuedSecondGesture=false,touchY=null,touchT=0,touchActive=false;function metrics(){const r=track.getBoundingClientRect(),total=track.offsetHeight-innerHeight,top=scrollY+r.top;return{top,total,endY:top+total}}function progressFromY(y){const m=metrics();return clamp((y-m.top)/Math.max(m.total,1))}function wheelPixels(e){if(e.deltaMode===1)return e.deltaY*18;if(e.deltaMode===2)return e.deltaY*innerHeight;return e.deltaY}function recordVelocity(deltaPx,now=performance.now()){const dt=lastInputAt?clamp(now-lastInputAt,8,120):32,instant=Math.abs(deltaPx)/dt;velocityEMA=velocityEMA*.68+instant*.32;lastInputAt=now}function adaptiveHandoffMs(){const speed=clamp((velocityEMA-.55)/(3.2-.55));return Math.round(mix(SLOW_HANDOFF_MS,FAST_HANDOFF_MS,smooth(speed)))}function armGestureEnd(now=performance.now()){quarantine=true;lastBlockedWheelAt=now;clearTimeout(quarantineTimer);quarantineTimer=setTimeout(()=>{quarantine=false},WHEEL_GESTURE_END_MS)}function touchGestureEnded(){touchActive=false;setTimeout(()=>{if(!touchActive)quarantine=false},60)}function serviceVisibility(p){if(handoffRunning)return handoff;if(handoffDone)return map(p,.94,1);return 0}function render(){raf=0;const p=progressFromY(scrollY),videoP=Math.pow(map(p,.018,.88),1.10);if(video.readyState>=1){const t=videoP*DURATION;if(Math.abs(video.currentTime-t)>.010)video.currentTime=t}const tf=map(p,.28,.62);copy.style.opacity=1-tf;copy.style.transform=`translateY(calc(-43% - ${tf*30}px))`;hint.style.opacity=1-map(p,.04,.18);wash.style.opacity=map(p,.78,.94);shade.style.opacity=1-map(p,.58,.86);const sv=serviceVisibility(p);stage.style.opacity=sv;stage.classList.toggle('ready',sv>.98);services.style.opacity=sv;services.style.transform=`translateY(${(1-sv)*18}px)`;const darkOut=map(p,.52,.79),lightIn=sv;nav.style.opacity=clamp(1-darkOut+lightIn);nav.classList.toggle('light',lightIn>.45);if(p<RESET_P&&!handoffRunning){handoff=0;handoffDone=false;velocityEMA=0}if(p>.9985&&!handoffRunning&&!handoffDone)startHandoff()}function schedule(){if(!raf)raf=requestAnimationFrame(render)}function startHandoff(){if(handoffRunning||handoffDone)return;const duration=adaptiveHandoffMs();handoffRunning=true;handoffDone=false;handoff=0;armGestureEnd();const start=performance.now();function tick(now){const t=clamp((now-start)/duration);handoff=smooth(t);schedule();if(t<1){handoffRaf=requestAnimationFrame(tick)}else{handoff=1;handoffRunning=false;handoffDone=true;quarantine=false;queuedSecondGesture=false;clearTimeout(quarantineTimer);schedule()}}handoffRaf=requestAnimationFrame(tick)}addEventListener('scroll',schedule,{passive:true});addEventListener('resize',schedule);addEventListener('wheel',e=>{const dy=wheelPixels(e),m=metrics(),y=scrollY,now=performance.now();if(handoffRunning){const gap=lastBlockedWheelAt?now-lastBlockedWheelAt:0;if(dy>0&&gap>=NEW_GESTURE_GAP_MS)queuedSecondGesture=true;e.preventDefault();armGestureEnd(now);return}if(quarantine){const gap=lastBlockedWheelAt?now-lastBlockedWheelAt:0,freshSecondGesture=handoffDone&&dy>0&&gap>=NEW_GESTURE_GAP_MS;if(freshSecondGesture){quarantine=false;queuedSecondGesture=false;clearTimeout(quarantineTimer);return}e.preventDefault();armGestureEnd(now);return}if(handoffDone)return;if(dy<=0)return;if(y<m.endY-1){e.preventDefault();recordVelocity(dy);const next=Math.min(m.endY,y+Math.max(1,dy));scrollTo(0,next);schedule();if(next>=m.endY-1)startHandoff()}else{e.preventDefault();startHandoff()}},{passive:false});addEventListener('touchstart',e=>{touchY=e.touches[0]?.clientY??null;touchT=performance.now();touchActive=true;if(handoffRunning&&quarantine)queuedSecondGesture=true;if(handoffDone&&quarantine){quarantine=false;clearTimeout(quarantineTimer)}},{passive:true});addEventListener('touchmove',e=>{if(touchY==null)return;const nowY=e.touches[0]?.clientY??touchY,nowT=performance.now(),dy=touchY-nowY;touchY=nowY;const dt=Math.max(8,nowT-touchT);touchT=nowT;const m=metrics(),y=scrollY;if(handoffRunning||quarantine){e.preventDefault();return}if(handoffDone)return;if(dy<=0)return;if(y<m.endY-1){e.preventDefault();const instant=Math.abs(dy)/dt;velocityEMA=velocityEMA*.68+instant*.32;lastInputAt=nowT;const next=Math.min(m.endY,y+dy);scrollTo(0,next);schedule();if(next>=m.endY-1){quarantine=true;startHandoff()}}else{e.preventDefault();quarantine=true;startHandoff()}},{passive:false});addEventListener('touchend',()=>{touchY=null;touchGestureEnded()},{passive:true});addEventListener('touchcancel',()=>{touchY=null;touchGestureEnded()},{passive:true});video.addEventListener('loadedmetadata',schedule);schedule()})();